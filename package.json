{
  "name": "claudio-cammarano-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --serve",
    "prestart": "node scripts/sync-concepts.js && node scripts/validate-concepts.js && node scripts/check-tags.js && node scripts/validate-geo.js",
    "prebuild": "node scripts/sync-concepts.js && node scripts/validate-concepts.js && node scripts/check-tags.js && node scripts/validate-geo.js",
    "build": "eleventy && (npx pagefind --site _site || true)",
    "index": "npx pagefind --site _site",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "pagefind": "^1.5.2",
    "sharp": "^0.35.4"
  }
}
